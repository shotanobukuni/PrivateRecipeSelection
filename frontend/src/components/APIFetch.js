import React, {useReducer} from 'react'

const initialState = []
const reducer = (currentState, action) => {
    switch (action) {
        case 'POST':
        case 'PUT':
        case 'DELETE':

        default:
            break;
    }
}

function APIFetch() {
    return (
        <div>
            
        </div>
    )
}

export default APIFetch
